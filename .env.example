# Google Drive Sync Configuration Example

# Local folder to sync (absolute path)
LOCAL_FOLDER=/home/aritrarc1/GDrive

# Google Drive folder name (will be created if doesn't exist)
DRIVE_FOLDER_NAME=GDriveSync

# Sync interval in seconds (default: 300 = 5 minutes)
SYNC_INTERVAL=300

# OAuth credentials file path (relative to project root)
CREDS_FILE=secrets/client_secret_116989766183-v0e50u650rdhah0j933fsloke77hp1od.apps.googleusercontent.com.json
